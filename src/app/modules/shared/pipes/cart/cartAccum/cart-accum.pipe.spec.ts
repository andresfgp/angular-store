import { CartAccumPipe } from './cart-accum.pipe';

describe('CartAccumPipe', () => {
  it('create an instance', () => {
    const pipe = new CartAccumPipe();
    expect(pipe).toBeTruthy();
  });
});
